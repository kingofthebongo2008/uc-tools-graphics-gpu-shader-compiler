namespace UniqueCreator
{
    namespace Graphics
    {
        namespace Gpu
        {
            namespace Shaders
            {
                namespace my_shader_type
                {
                    public sealed class Factory
                    {
                        public static GeometryShaderByteCode Create()
                        {
                            var code = new GeometryShaderByteCode();
                            var bytes = new byte[]
                            {
                                    my_shader_type_blob
                            };
                            code.Code = bytes;
                            return code;
                        }
                    }
                }
            }
        }
    }
}

